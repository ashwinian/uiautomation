import { Options } from './types';
interface Option {
    type: 'string' | 'number' | 'object' | 'boolean' | 'function';
    validate?: (option: any) => boolean;
    default?: any;
    match?: RegExp;
    required?: boolean;
}
export declare const DEFAULTS: Record<Exclude<keyof Options, 'requestedCapabilities'>, Option>;
export {};
//# sourceMappingURL=constants.d.ts.map